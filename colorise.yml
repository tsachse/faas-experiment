provider:
  name: faas
  gateway: http://tsachse720:8080

functions:
  colorization:
    lang: Dockerfile
    handler: ./colorise
    image: openfaas-colorization
    environment:
      read_timeout: 60
      write_timeout: 60
    environment_file:
      - credentials.yml
